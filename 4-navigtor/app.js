const addressLat = 10;
const addressLong = 20;
const positionLat = 30;
const positionLong = 40;

const distance = Math.sqrt(Math.pow(addressLat - positionLat, 2) + Math.pow(addressLong - positionLong, 2));